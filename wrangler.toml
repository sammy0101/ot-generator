name = "ot-generator"
main = "src/index.js"
compatibility_date = "2023-12-01"

# KV 設定
[[kv_namespaces]]
binding = "OT_RECORDS"
id = "REPLACE_ME_KV_ID"  # <--- 這裡維持佔位符

# === 修改重點：加入 vars 區塊 ===
[vars]
USER_NAME = "REPLACE_ME_NAME"  # <--- 這裡放名字佔位符
