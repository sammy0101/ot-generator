name = "ot-generator"
main = "src/index.js"
compatibility_date = "2023-12-01"

# === KV 設定 ===
[[kv_namespaces]]
binding = "OT_RECORDS"
id = "REPLACE_ME_KV_ID"  # <--- 這裡改成佔位符，不要留真實 ID

# [vars] 區塊記得要是空的或已刪除 (因為密碼已經移走了)
